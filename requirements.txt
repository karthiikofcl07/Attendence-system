Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Limiter==3.7.0
SQLAlchemy==2.0.30
Werkzeug==3.0.3
setuptools<81

# --- Computer vision stack ---
# IMPORTANT: install via install.sh / install.bat, NOT a plain `pip install -r requirements.txt`.
# face_recognition's own metadata requires the "dlib" PyPI package by name, which has no
# universal pre-built wheel and triggers a slow cmake/C++ source build. dlib-bin below
# provides a real, working `dlib` module via a precompiled wheel instead -- but pip's
# dependency resolver won't know the two are interchangeable, so face_recognition must be
# installed with --no-deps (the install script does this for you). See README.md.
dlib-bin==20.0.1
opencv-python==4.10.0.84
face_recognition_models==0.3.0
Click>=6.0
numpy==1.26.4
Pillow==10.3.0

pandas==2.2.2
APScheduler==3.10.4
openpyxl==3.1.2
reportlab==4.2.2
python-dotenv==1.0.1

# Installed separately with --no-deps by install.sh / install.bat:
# face_recognition==1.3.0
